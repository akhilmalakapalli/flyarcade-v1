"""FlyArcade v1.5 performance development (kept outside ``src/``).

The v1.4 provenance hash covers every ``src/**/*.py`` file, so v1.5 code lives in
``src_v15`` to keep the frozen v1.4 audit reproducible on this branch. Scripts put
``src_v15`` on ``sys.path`` explicitly.
"""
