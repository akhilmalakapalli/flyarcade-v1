"""FlyArcade v1.3 adaptive-learning extension.

Kept outside ``src/flyarcade`` on purpose: the frozen v1.2 code hash covers every
module under that package, so a sibling package leaves every v1/v1.1/v1.2
executable file, run plan and audit byte-identical and still verifiable.
"""

VERSION = "1.3"
